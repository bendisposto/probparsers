(ns btypes.core
  (:require [clojure.set :as s])
  (:require [btypes.bobject])
  (:require [btypes.bboolean])
  (:require [btypes.binteger])
  (:require [btypes.bcouple])
  (:require [btypes.bset])
  (:require [btypes.butils]))


;(keep #({1 2 3 4} %) [1 2 3])

;(de.hhu.stups.btypes.BBSet. (BSet. (into-array [(BInteger. "1")])))
;(map type (range (BInteger. "2")))
