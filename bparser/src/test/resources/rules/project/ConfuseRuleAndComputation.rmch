RULES_MACHINE ConfuseRuleAndComputation
OPERATIONS

RULE rule1
BODY
  RULE_FAIL
  WHEN 1=2
  COUNTEREXAMPLE "never"
  END
END
;
COMPUTATION compute_x
DEPENDS_ON_COMPUTATION rule1
BODY
  DEFINE
    def
  TYPE
    POW(INTEGER)
  VALUE
    UNION(x).(x : 1..10 | {x + x })
  END//DEFINE
END//COMPUTATION

END
